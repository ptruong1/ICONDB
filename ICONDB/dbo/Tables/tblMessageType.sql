CREATE TABLE [dbo].[tblMessageType] (
    [MessageTypeID] TINYINT      NOT NULL,
    [Descript]      VARCHAR (25) NOT NULL,
    CONSTRAINT [PK_tblMessageType] PRIMARY KEY CLUSTERED ([MessageTypeID] ASC)
);

