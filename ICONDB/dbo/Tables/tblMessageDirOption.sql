CREATE TABLE [dbo].[tblMessageDirOption] (
    [MessageDirOpt] TINYINT      NOT NULL,
    [Descript]      VARCHAR (30) NULL,
    CONSTRAINT [PK_tblMessageDirOption] PRIMARY KEY CLUSTERED ([MessageDirOpt] ASC)
);

